@model MooshakV2.ViewModels.UserDetailViewModel

@{
    ViewBag.Title = "delete";
}

<h2>delete</h2>

<h3>Are you sure you want to delete this user?</h3>
<div>
    <h4>@Html.HiddenFor(m => m.userModel.userName)</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.HiddenFor(m => m.userModel.email)
        </dt>

        <dd>
            @Html.HiddenFor(m => m.userModel.roleName)
        </dd>

        <dt>
            @Html.HiddenFor(m => m.userModel)
        </dt>

        <dd>
            @Html.DisplayNameFor(model => model.name)
        </dd>

        <dt>
            @Html.DisplayFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayNameFor(model => model.ssn)
        </dd>

        <dt>
            @Html.DisplayFor(model => model.ssn)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.userModel.userName)
        </dd>

    </dl>

    @using (Html.BeginForm(FormMethod.Post, new string {))
    {
        @Html.AntiForgeryToken()

        <div class="btn-group btn-group-sm" role="group" aria-label="...">
            <input type="submit" value="Delete" class="btn btn-default" />
            @Html.ActionLink("Back to List", "Index", new { }, new { @class = "btn btn-default" })
        </div>
    }
</div>
