@model IEnumerable<MooshakV2.ViewModels.UserDetailViewModel>

@{
    ViewBag.Title = "User List";
}

<h2>User List</h2>

<p>
    <a href="@Url.Action("Create")" class="btn btn-default" data-toggle="tooltip" data-placement="right" title="Create new user">
        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
    </a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(m => m.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.userModel.userName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.userModel.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.userModel.roleName)
        </th>
        <th></th>
    </tr>

    @foreach(var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(m => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userModel.userName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userModel.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userModel.roleName)
            </td>
            <td>
                <div class="btn-group btn-group-sm" role="group" aria-label="...">
                    <a href="@Url.Action("Edit", new {userName = item.userModel.userName})"
                       class="btn btn-default"
                       data-toggle="tooltip"
                       data-placement="left"
                       title="Edit user information">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    </a>
                    <a href="@Url.Action("Details", new {userName = item.userModel.userName})"
                       class="btn btn-default"
                       data-toggle="tooltip"
                       data-placement="top"
                       title="About this user">
                        <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
                    </a>
                    <a href="@Url.Action("Delete", new {userName = item.userModel.userName})"
                       class="btn btn-default"
                       data-toggle="tooltip"
                       data-placement="right"
                       title="Delete user">
                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    </a>
                </div>
            </td>
        </tr>
    }

</table>


@section scripts
{
    @Scripts.Render("~/Scripts/renderTooltips.js")
}
